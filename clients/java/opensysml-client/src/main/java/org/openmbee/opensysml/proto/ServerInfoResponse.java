// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: sysml.proto
// Protobuf Java Version: 4.33.1

package org.openmbee.opensysml.proto;

/**
 * <pre>
 * ServerInfoResponse describes the running service.
 * </pre>
 *
 * Protobuf type {@code sysml.ServerInfoResponse}
 */
@com.google.protobuf.Generated
public final class ServerInfoResponse extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:sysml.ServerInfoResponse)
    ServerInfoResponseOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 33,
      /* patch= */ 1,
      /* suffix= */ "",
      "ServerInfoResponse");
  }
  // Use ServerInfoResponse.newBuilder() to construct.
  private ServerInfoResponse(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private ServerInfoResponse() {
    version_ = "";
    capabilities_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return org.openmbee.opensysml.proto.Sysml.internal_static_sysml_ServerInfoResponse_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return org.openmbee.opensysml.proto.Sysml.internal_static_sysml_ServerInfoResponse_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            org.openmbee.opensysml.proto.ServerInfoResponse.class, org.openmbee.opensysml.proto.ServerInfoResponse.Builder.class);
  }

  public static final int VERSION_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private volatile java.lang.Object version_ = "";
  /**
   * <pre>
   * Build version of the service binary, informational only: it is "dev" for a
   * build without release ldflags, and versions of forks are not comparable.
   * Feature decisions belong in `capabilities`.
   * </pre>
   *
   * <code>string version = 1 [json_name = "version"];</code>
   * @return The version.
   */
  @java.lang.Override
  public java.lang.String getVersion() {
    java.lang.Object ref = version_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      version_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * Build version of the service binary, informational only: it is "dev" for a
   * build without release ldflags, and versions of forks are not comparable.
   * Feature decisions belong in `capabilities`.
   * </pre>
   *
   * <code>string version = 1 [json_name = "version"];</code>
   * @return The bytes for version.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getVersionBytes() {
    java.lang.Object ref = version_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      version_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int CAPABILITIES_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList capabilities_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * Named capabilities this build supports, each an exact string a client can
   * require. Capabilities are only ever added, never renamed or removed with
   * their behaviour intact, so requiring one is stable across releases.
   * Currently defined:
   * "type_facts" - SymbolInfo carries type_info, multiplicity and
   * specializations, which typed code generation needs.
   * "convert"    - the Convert RPC writes a model back out as SysML notation
   * or RDF Turtle.
   * "verification" - the VerifyConstraint, VerifyRequirement,
   * VerifySatisfaction and EvaluateCalc RPCs answer the
   * questions the REPL's %constraint, %requirement, %satisfy
   * and %calc answer.
   * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
   * "oslc_query" - the Query RPC evaluates OSLC Query text.
   * "enum_values" - a Value carries an enumeration literal as enum_literal,
   * rather than reporting it as an unsupported null.
   * "unset_value" - a valueless feature of a value type is reported as
   * Value.unset, rather than as the empty object it
   * materializes.
   * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
   * RunAnalysis RPCs report what the body of a verification
   * case answered as verification_verdicts, and RunAnalysis
   * accepts a verification case.
   * "complex_values" - a Value carries a complex number as complex, rather
   * than reporting it as an unsupported null, and a complex
   * action input or calc argument is accepted; without it,
   * one is refused with UNIMPLEMENTED rather than read as
   * another value.
   * "structured_values" - a Value carries a Collections::Array, a numerical
   * vector and a vector quantity as array, vector and
   * vector_quantity, shape and units intact, rather than
   * reporting them as unsupported nulls, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value.
   * "measurement_refs" - a Value carries a bare measurement reference (a
   * unit by itself, `SI::m` or `m / s`) as measurement_ref,
   * unit text, reduction and declaration intact, rather than
   * reporting it as an unsupported null, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value. Separate from structured_values, which a client
   * built before this arm existed may already claim.
   * "function_values" - a Value carries a calc held as a value as function,
   * named by its declaration, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "set_values" - a Value carries a unique, unordered collection (a
   * Collections::Set's elements) as set, each element once in
   * canonical order, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument in any order; without it, one is refused
   * with UNIMPLEMENTED rather than read as a sequence.
   * "tensor_values" - a Value carries a tensor quantity of any rank as
   * tensor_quantity, its dimensions and one Quantity per
   * row-major component, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
   * preserving everything the edit did not touch.
   * "document_query" - the RunDocumentQuery RPC runs a named document query
   * and answers with typed rows.
   * "render_document" - the RenderDocument RPC renders a named document to
   * Markdown.
   * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
   * finding none was assigned; without it every code is empty.
   * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
   * RunAnalysisRequest take a schedule, the scheduling policy
   * the run resolves its choice points under; without it a
   * service drops the field and runs under the default, so a
   * client must not send one.
   * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
   * accepted, and the response carries every distinct outcome
   * as `outcomes` with an `exploration` status; without it the
   * spelling is INVALID_ARGUMENT.
   * </pre>
   *
   * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
   * @return A list containing the capabilities.
   */
  public com.google.protobuf.ProtocolStringList
      getCapabilitiesList() {
    return capabilities_;
  }
  /**
   * <pre>
   * Named capabilities this build supports, each an exact string a client can
   * require. Capabilities are only ever added, never renamed or removed with
   * their behaviour intact, so requiring one is stable across releases.
   * Currently defined:
   * "type_facts" - SymbolInfo carries type_info, multiplicity and
   * specializations, which typed code generation needs.
   * "convert"    - the Convert RPC writes a model back out as SysML notation
   * or RDF Turtle.
   * "verification" - the VerifyConstraint, VerifyRequirement,
   * VerifySatisfaction and EvaluateCalc RPCs answer the
   * questions the REPL's %constraint, %requirement, %satisfy
   * and %calc answer.
   * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
   * "oslc_query" - the Query RPC evaluates OSLC Query text.
   * "enum_values" - a Value carries an enumeration literal as enum_literal,
   * rather than reporting it as an unsupported null.
   * "unset_value" - a valueless feature of a value type is reported as
   * Value.unset, rather than as the empty object it
   * materializes.
   * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
   * RunAnalysis RPCs report what the body of a verification
   * case answered as verification_verdicts, and RunAnalysis
   * accepts a verification case.
   * "complex_values" - a Value carries a complex number as complex, rather
   * than reporting it as an unsupported null, and a complex
   * action input or calc argument is accepted; without it,
   * one is refused with UNIMPLEMENTED rather than read as
   * another value.
   * "structured_values" - a Value carries a Collections::Array, a numerical
   * vector and a vector quantity as array, vector and
   * vector_quantity, shape and units intact, rather than
   * reporting them as unsupported nulls, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value.
   * "measurement_refs" - a Value carries a bare measurement reference (a
   * unit by itself, `SI::m` or `m / s`) as measurement_ref,
   * unit text, reduction and declaration intact, rather than
   * reporting it as an unsupported null, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value. Separate from structured_values, which a client
   * built before this arm existed may already claim.
   * "function_values" - a Value carries a calc held as a value as function,
   * named by its declaration, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "set_values" - a Value carries a unique, unordered collection (a
   * Collections::Set's elements) as set, each element once in
   * canonical order, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument in any order; without it, one is refused
   * with UNIMPLEMENTED rather than read as a sequence.
   * "tensor_values" - a Value carries a tensor quantity of any rank as
   * tensor_quantity, its dimensions and one Quantity per
   * row-major component, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
   * preserving everything the edit did not touch.
   * "document_query" - the RunDocumentQuery RPC runs a named document query
   * and answers with typed rows.
   * "render_document" - the RenderDocument RPC renders a named document to
   * Markdown.
   * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
   * finding none was assigned; without it every code is empty.
   * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
   * RunAnalysisRequest take a schedule, the scheduling policy
   * the run resolves its choice points under; without it a
   * service drops the field and runs under the default, so a
   * client must not send one.
   * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
   * accepted, and the response carries every distinct outcome
   * as `outcomes` with an `exploration` status; without it the
   * spelling is INVALID_ARGUMENT.
   * </pre>
   *
   * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
   * @return The count of capabilities.
   */
  public int getCapabilitiesCount() {
    return capabilities_.size();
  }
  /**
   * <pre>
   * Named capabilities this build supports, each an exact string a client can
   * require. Capabilities are only ever added, never renamed or removed with
   * their behaviour intact, so requiring one is stable across releases.
   * Currently defined:
   * "type_facts" - SymbolInfo carries type_info, multiplicity and
   * specializations, which typed code generation needs.
   * "convert"    - the Convert RPC writes a model back out as SysML notation
   * or RDF Turtle.
   * "verification" - the VerifyConstraint, VerifyRequirement,
   * VerifySatisfaction and EvaluateCalc RPCs answer the
   * questions the REPL's %constraint, %requirement, %satisfy
   * and %calc answer.
   * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
   * "oslc_query" - the Query RPC evaluates OSLC Query text.
   * "enum_values" - a Value carries an enumeration literal as enum_literal,
   * rather than reporting it as an unsupported null.
   * "unset_value" - a valueless feature of a value type is reported as
   * Value.unset, rather than as the empty object it
   * materializes.
   * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
   * RunAnalysis RPCs report what the body of a verification
   * case answered as verification_verdicts, and RunAnalysis
   * accepts a verification case.
   * "complex_values" - a Value carries a complex number as complex, rather
   * than reporting it as an unsupported null, and a complex
   * action input or calc argument is accepted; without it,
   * one is refused with UNIMPLEMENTED rather than read as
   * another value.
   * "structured_values" - a Value carries a Collections::Array, a numerical
   * vector and a vector quantity as array, vector and
   * vector_quantity, shape and units intact, rather than
   * reporting them as unsupported nulls, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value.
   * "measurement_refs" - a Value carries a bare measurement reference (a
   * unit by itself, `SI::m` or `m / s`) as measurement_ref,
   * unit text, reduction and declaration intact, rather than
   * reporting it as an unsupported null, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value. Separate from structured_values, which a client
   * built before this arm existed may already claim.
   * "function_values" - a Value carries a calc held as a value as function,
   * named by its declaration, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "set_values" - a Value carries a unique, unordered collection (a
   * Collections::Set's elements) as set, each element once in
   * canonical order, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument in any order; without it, one is refused
   * with UNIMPLEMENTED rather than read as a sequence.
   * "tensor_values" - a Value carries a tensor quantity of any rank as
   * tensor_quantity, its dimensions and one Quantity per
   * row-major component, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
   * preserving everything the edit did not touch.
   * "document_query" - the RunDocumentQuery RPC runs a named document query
   * and answers with typed rows.
   * "render_document" - the RenderDocument RPC renders a named document to
   * Markdown.
   * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
   * finding none was assigned; without it every code is empty.
   * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
   * RunAnalysisRequest take a schedule, the scheduling policy
   * the run resolves its choice points under; without it a
   * service drops the field and runs under the default, so a
   * client must not send one.
   * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
   * accepted, and the response carries every distinct outcome
   * as `outcomes` with an `exploration` status; without it the
   * spelling is INVALID_ARGUMENT.
   * </pre>
   *
   * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
   * @param index The index of the element to return.
   * @return The capabilities at the given index.
   */
  public java.lang.String getCapabilities(int index) {
    return capabilities_.get(index);
  }
  /**
   * <pre>
   * Named capabilities this build supports, each an exact string a client can
   * require. Capabilities are only ever added, never renamed or removed with
   * their behaviour intact, so requiring one is stable across releases.
   * Currently defined:
   * "type_facts" - SymbolInfo carries type_info, multiplicity and
   * specializations, which typed code generation needs.
   * "convert"    - the Convert RPC writes a model back out as SysML notation
   * or RDF Turtle.
   * "verification" - the VerifyConstraint, VerifyRequirement,
   * VerifySatisfaction and EvaluateCalc RPCs answer the
   * questions the REPL's %constraint, %requirement, %satisfy
   * and %calc answer.
   * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
   * "oslc_query" - the Query RPC evaluates OSLC Query text.
   * "enum_values" - a Value carries an enumeration literal as enum_literal,
   * rather than reporting it as an unsupported null.
   * "unset_value" - a valueless feature of a value type is reported as
   * Value.unset, rather than as the empty object it
   * materializes.
   * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
   * RunAnalysis RPCs report what the body of a verification
   * case answered as verification_verdicts, and RunAnalysis
   * accepts a verification case.
   * "complex_values" - a Value carries a complex number as complex, rather
   * than reporting it as an unsupported null, and a complex
   * action input or calc argument is accepted; without it,
   * one is refused with UNIMPLEMENTED rather than read as
   * another value.
   * "structured_values" - a Value carries a Collections::Array, a numerical
   * vector and a vector quantity as array, vector and
   * vector_quantity, shape and units intact, rather than
   * reporting them as unsupported nulls, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value.
   * "measurement_refs" - a Value carries a bare measurement reference (a
   * unit by itself, `SI::m` or `m / s`) as measurement_ref,
   * unit text, reduction and declaration intact, rather than
   * reporting it as an unsupported null, and one is accepted
   * as an action input or calc argument; without it, one is
   * refused with UNIMPLEMENTED rather than read as another
   * value. Separate from structured_values, which a client
   * built before this arm existed may already claim.
   * "function_values" - a Value carries a calc held as a value as function,
   * named by its declaration, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "set_values" - a Value carries a unique, unordered collection (a
   * Collections::Set's elements) as set, each element once in
   * canonical order, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument in any order; without it, one is refused
   * with UNIMPLEMENTED rather than read as a sequence.
   * "tensor_values" - a Value carries a tensor quantity of any rank as
   * tensor_quantity, its dimensions and one Quantity per
   * row-major component, rather than reporting it as an
   * unsupported null, and one is accepted as an action input
   * or calc argument; without it, one is refused with
   * UNIMPLEMENTED rather than read as another value.
   * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
   * preserving everything the edit did not touch.
   * "document_query" - the RunDocumentQuery RPC runs a named document query
   * and answers with typed rows.
   * "render_document" - the RenderDocument RPC renders a named document to
   * Markdown.
   * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
   * finding none was assigned; without it every code is empty.
   * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
   * RunAnalysisRequest take a schedule, the scheduling policy
   * the run resolves its choice points under; without it a
   * service drops the field and runs under the default, so a
   * client must not send one.
   * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
   * accepted, and the response carries every distinct outcome
   * as `outcomes` with an `exploration` status; without it the
   * spelling is INVALID_ARGUMENT.
   * </pre>
   *
   * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
   * @param index The index of the value to return.
   * @return The bytes of the capabilities at the given index.
   */
  public com.google.protobuf.ByteString
      getCapabilitiesBytes(int index) {
    return capabilities_.getByteString(index);
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(version_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, version_);
    }
    for (int i = 0; i < capabilities_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, capabilities_.getRaw(i));
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(version_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, version_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < capabilities_.size(); i++) {
        dataSize += computeStringSizeNoTag(capabilities_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getCapabilitiesList().size();
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof org.openmbee.opensysml.proto.ServerInfoResponse)) {
      return super.equals(obj);
    }
    org.openmbee.opensysml.proto.ServerInfoResponse other = (org.openmbee.opensysml.proto.ServerInfoResponse) obj;

    if (!getVersion()
        .equals(other.getVersion())) return false;
    if (!getCapabilitiesList()
        .equals(other.getCapabilitiesList())) return false;
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + VERSION_FIELD_NUMBER;
    hash = (53 * hash) + getVersion().hashCode();
    if (getCapabilitiesCount() > 0) {
      hash = (37 * hash) + CAPABILITIES_FIELD_NUMBER;
      hash = (53 * hash) + getCapabilitiesList().hashCode();
    }
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static org.openmbee.opensysml.proto.ServerInfoResponse parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static org.openmbee.opensysml.proto.ServerInfoResponse parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static org.openmbee.opensysml.proto.ServerInfoResponse parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(org.openmbee.opensysml.proto.ServerInfoResponse prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * ServerInfoResponse describes the running service.
   * </pre>
   *
   * Protobuf type {@code sysml.ServerInfoResponse}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:sysml.ServerInfoResponse)
      org.openmbee.opensysml.proto.ServerInfoResponseOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.openmbee.opensysml.proto.Sysml.internal_static_sysml_ServerInfoResponse_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return org.openmbee.opensysml.proto.Sysml.internal_static_sysml_ServerInfoResponse_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              org.openmbee.opensysml.proto.ServerInfoResponse.class, org.openmbee.opensysml.proto.ServerInfoResponse.Builder.class);
    }

    // Construct using org.openmbee.opensysml.proto.ServerInfoResponse.newBuilder()
    private Builder() {

    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);

    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      version_ = "";
      capabilities_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return org.openmbee.opensysml.proto.Sysml.internal_static_sysml_ServerInfoResponse_descriptor;
    }

    @java.lang.Override
    public org.openmbee.opensysml.proto.ServerInfoResponse getDefaultInstanceForType() {
      return org.openmbee.opensysml.proto.ServerInfoResponse.getDefaultInstance();
    }

    @java.lang.Override
    public org.openmbee.opensysml.proto.ServerInfoResponse build() {
      org.openmbee.opensysml.proto.ServerInfoResponse result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public org.openmbee.opensysml.proto.ServerInfoResponse buildPartial() {
      org.openmbee.opensysml.proto.ServerInfoResponse result = new org.openmbee.opensysml.proto.ServerInfoResponse(this);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartial0(org.openmbee.opensysml.proto.ServerInfoResponse result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.version_ = version_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        capabilities_.makeImmutable();
        result.capabilities_ = capabilities_;
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof org.openmbee.opensysml.proto.ServerInfoResponse) {
        return mergeFrom((org.openmbee.opensysml.proto.ServerInfoResponse)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(org.openmbee.opensysml.proto.ServerInfoResponse other) {
      if (other == org.openmbee.opensysml.proto.ServerInfoResponse.getDefaultInstance()) return this;
      if (!other.getVersion().isEmpty()) {
        version_ = other.version_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.capabilities_.isEmpty()) {
        if (capabilities_.isEmpty()) {
          capabilities_ = other.capabilities_;
          bitField0_ |= 0x00000002;
        } else {
          ensureCapabilitiesIsMutable();
          capabilities_.addAll(other.capabilities_);
        }
        onChanged();
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              version_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 18: {
              java.lang.String s = input.readStringRequireUtf8();
              ensureCapabilitiesIsMutable();
              capabilities_.add(s);
              break;
            } // case 18
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private java.lang.Object version_ = "";
    /**
     * <pre>
     * Build version of the service binary, informational only: it is "dev" for a
     * build without release ldflags, and versions of forks are not comparable.
     * Feature decisions belong in `capabilities`.
     * </pre>
     *
     * <code>string version = 1 [json_name = "version"];</code>
     * @return The version.
     */
    public java.lang.String getVersion() {
      java.lang.Object ref = version_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        version_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * Build version of the service binary, informational only: it is "dev" for a
     * build without release ldflags, and versions of forks are not comparable.
     * Feature decisions belong in `capabilities`.
     * </pre>
     *
     * <code>string version = 1 [json_name = "version"];</code>
     * @return The bytes for version.
     */
    public com.google.protobuf.ByteString
        getVersionBytes() {
      java.lang.Object ref = version_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        version_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * Build version of the service binary, informational only: it is "dev" for a
     * build without release ldflags, and versions of forks are not comparable.
     * Feature decisions belong in `capabilities`.
     * </pre>
     *
     * <code>string version = 1 [json_name = "version"];</code>
     * @param value The version to set.
     * @return This builder for chaining.
     */
    public Builder setVersion(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      version_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Build version of the service binary, informational only: it is "dev" for a
     * build without release ldflags, and versions of forks are not comparable.
     * Feature decisions belong in `capabilities`.
     * </pre>
     *
     * <code>string version = 1 [json_name = "version"];</code>
     * @return This builder for chaining.
     */
    public Builder clearVersion() {
      version_ = getDefaultInstance().getVersion();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Build version of the service binary, informational only: it is "dev" for a
     * build without release ldflags, and versions of forks are not comparable.
     * Feature decisions belong in `capabilities`.
     * </pre>
     *
     * <code>string version = 1 [json_name = "version"];</code>
     * @param value The bytes for version to set.
     * @return This builder for chaining.
     */
    public Builder setVersionBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      version_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private com.google.protobuf.LazyStringArrayList capabilities_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureCapabilitiesIsMutable() {
      if (!capabilities_.isModifiable()) {
        capabilities_ = new com.google.protobuf.LazyStringArrayList(capabilities_);
      }
      bitField0_ |= 0x00000002;
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @return A list containing the capabilities.
     */
    public com.google.protobuf.ProtocolStringList
        getCapabilitiesList() {
      capabilities_.makeImmutable();
      return capabilities_;
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @return The count of capabilities.
     */
    public int getCapabilitiesCount() {
      return capabilities_.size();
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @param index The index of the element to return.
     * @return The capabilities at the given index.
     */
    public java.lang.String getCapabilities(int index) {
      return capabilities_.get(index);
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @param index The index of the value to return.
     * @return The bytes of the capabilities at the given index.
     */
    public com.google.protobuf.ByteString
        getCapabilitiesBytes(int index) {
      return capabilities_.getByteString(index);
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @param index The index to set the value at.
     * @param value The capabilities to set.
     * @return This builder for chaining.
     */
    public Builder setCapabilities(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureCapabilitiesIsMutable();
      capabilities_.set(index, value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @param value The capabilities to add.
     * @return This builder for chaining.
     */
    public Builder addCapabilities(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureCapabilitiesIsMutable();
      capabilities_.add(value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @param values The capabilities to add.
     * @return This builder for chaining.
     */
    public Builder addAllCapabilities(
        java.lang.Iterable<java.lang.String> values) {
      ensureCapabilitiesIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, capabilities_);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @return This builder for chaining.
     */
    public Builder clearCapabilities() {
      capabilities_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000002);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * Named capabilities this build supports, each an exact string a client can
     * require. Capabilities are only ever added, never renamed or removed with
     * their behaviour intact, so requiring one is stable across releases.
     * Currently defined:
     * "type_facts" - SymbolInfo carries type_info, multiplicity and
     * specializations, which typed code generation needs.
     * "convert"    - the Convert RPC writes a model back out as SysML notation
     * or RDF Turtle.
     * "verification" - the VerifyConstraint, VerifyRequirement,
     * VerifySatisfaction and EvaluateCalc RPCs answer the
     * questions the REPL's %constraint, %requirement, %satisfy
     * and %calc answer.
     * "query"      - the Query RPC evaluates a SysML v2 API &amp; Services Query.
     * "oslc_query" - the Query RPC evaluates OSLC Query text.
     * "enum_values" - a Value carries an enumeration literal as enum_literal,
     * rather than reporting it as an unsupported null.
     * "unset_value" - a valueless feature of a value type is reported as
     * Value.unset, rather than as the empty object it
     * materializes.
     * "verification_verdicts" - the VerifyRequirement, VerifySatisfaction and
     * RunAnalysis RPCs report what the body of a verification
     * case answered as verification_verdicts, and RunAnalysis
     * accepts a verification case.
     * "complex_values" - a Value carries a complex number as complex, rather
     * than reporting it as an unsupported null, and a complex
     * action input or calc argument is accepted; without it,
     * one is refused with UNIMPLEMENTED rather than read as
     * another value.
     * "structured_values" - a Value carries a Collections::Array, a numerical
     * vector and a vector quantity as array, vector and
     * vector_quantity, shape and units intact, rather than
     * reporting them as unsupported nulls, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value.
     * "measurement_refs" - a Value carries a bare measurement reference (a
     * unit by itself, `SI::m` or `m / s`) as measurement_ref,
     * unit text, reduction and declaration intact, rather than
     * reporting it as an unsupported null, and one is accepted
     * as an action input or calc argument; without it, one is
     * refused with UNIMPLEMENTED rather than read as another
     * value. Separate from structured_values, which a client
     * built before this arm existed may already claim.
     * "function_values" - a Value carries a calc held as a value as function,
     * named by its declaration, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "set_values" - a Value carries a unique, unordered collection (a
     * Collections::Set's elements) as set, each element once in
     * canonical order, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument in any order; without it, one is refused
     * with UNIMPLEMENTED rather than read as a sequence.
     * "tensor_values" - a Value carries a tensor quantity of any rank as
     * tensor_quantity, its dimensions and one Quantity per
     * row-major component, rather than reporting it as an
     * unsupported null, and one is accepted as an action input
     * or calc argument; without it, one is refused with
     * UNIMPLEMENTED rather than read as another value.
     * "apply_edits" - the ApplyEdits RPC edits a parsed model's own source,
     * preserving everything the edit did not touch.
     * "document_query" - the RunDocumentQuery RPC runs a named document query
     * and answers with typed rows.
     * "render_document" - the RenderDocument RPC renders a named document to
     * Markdown.
     * "diagnostic_codes" - Diagnostic.code is populated, so an empty code is a
     * finding none was assigned; without it every code is empty.
     * "schedule"     - ExecuteActionRequest, ExecuteStateRequest and
     * RunAnalysisRequest take a schedule, the scheduling policy
     * the run resolves its choice points under; without it a
     * service drops the field and runs under the default, so a
     * client must not send one.
     * "schedule_explore" - the schedule "explore[:runs=&lt;n&gt;,depth=&lt;d&gt;]" is
     * accepted, and the response carries every distinct outcome
     * as `outcomes` with an `exploration` status; without it the
     * spelling is INVALID_ARGUMENT.
     * </pre>
     *
     * <code>repeated string capabilities = 2 [json_name = "capabilities"];</code>
     * @param value The bytes of the capabilities to add.
     * @return This builder for chaining.
     */
    public Builder addCapabilitiesBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureCapabilitiesIsMutable();
      capabilities_.add(value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    // @@protoc_insertion_point(builder_scope:sysml.ServerInfoResponse)
  }

  // @@protoc_insertion_point(class_scope:sysml.ServerInfoResponse)
  private static final org.openmbee.opensysml.proto.ServerInfoResponse DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new org.openmbee.opensysml.proto.ServerInfoResponse();
  }

  public static org.openmbee.opensysml.proto.ServerInfoResponse getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<ServerInfoResponse>
      PARSER = new com.google.protobuf.AbstractParser<ServerInfoResponse>() {
    @java.lang.Override
    public ServerInfoResponse parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<ServerInfoResponse> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<ServerInfoResponse> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public org.openmbee.opensysml.proto.ServerInfoResponse getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

