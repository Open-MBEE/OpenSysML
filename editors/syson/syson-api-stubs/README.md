# SysON API stubs

This module contains compile-only API stubs for the SysON 2026.9.0 APIs.
It lets the backend build without downloading authenticated SysON artifacts.
The stubs are not runtime implementations and concrete methods throw `"stub"`.
Use the `syson-artifacts` profile when building against the real SysON APIs.
