{
  "type": "state",
  "schedule": "reverse",
  "events": [{"signal": "Go"}],
  "finalState": "done",
  "outputs": {
    "log": {
      "type": "String",
      "value": "work(entry) a(entry) b(entry) a(exit) b(exit) T1(effect) T1.1(effect) c(entry) T1.2(effect) d(entry) work(exit) T2(effect) "
    }
  },
  "trace": true
}
