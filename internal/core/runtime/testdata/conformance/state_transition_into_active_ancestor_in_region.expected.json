{
  "type": "state",
  "trace": true,
  "events": [
    {"signal": "Up", "args": null},
    {"signal": "Leave", "args": null}
  ],
  "finalState": "done",
  "outputs": {
    "log": {"type": "String", "value": "mid(entry);inner(entry);b(entry);inner(exit);up(effect);mid(exit);completion(effect);leftDone(entry);b(exit);"}
  }
}
