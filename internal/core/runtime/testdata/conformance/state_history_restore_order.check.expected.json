{
	"verdict": "divergent",
	"divergent": {"log": ["\"l1(entry) r1(entry) paused(entry) l1(entry) r1(entry) \"", "\"l1(entry) r1(entry) paused(entry) r1(entry) l1(entry) \"", "\"r1(entry) l1(entry) paused(entry) l1(entry) r1(entry) \"", "\"r1(entry) l1(entry) paused(entry) r1(entry) l1(entry) \""]},
	"agreed": {"finalState": "l1+r1"}
}
