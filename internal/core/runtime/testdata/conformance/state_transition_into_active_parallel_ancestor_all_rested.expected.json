{
  "type": "state",
  "trace": true,
  "events": [
    {"signal": "Up", "args": null},
    {"signal": "Down", "args": null},
    {"signal": "Leave", "args": null},
    {"signal": "Finish", "args": null}
  ],
  "finalState": "done",
  "outputs": {
    "log": {"type": "String", "value": "outer(entry);a(entry);b(entry);a(exit);up(effect);b(exit);down(effect);outer(exit);leave(effect);later(entry);"}
  }
}
