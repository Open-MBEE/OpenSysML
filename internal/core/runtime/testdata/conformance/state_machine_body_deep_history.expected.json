{
  "type": "state",
  "trace": true,
  "events": [
    {"signal": "Go", "args": null},
    {"signal": "Next", "args": null},
    {"signal": "Again", "args": null},
    {"signal": "Shallow", "args": null},
    {"signal": "Next", "args": null},
    {"signal": "Finish", "args": null}
  ],
  "finalState": "done",
  "stateVisits": ["start", "outer", "first", "inner", "outer", "inner", "outer", "first", "inner", "done"],
  "outputs": {
    "log": {"type": "String", "value": "outer(entry);inner(entry);outer(exit);again(effect);outer(entry);inner(entry);outer(exit);shallow(effect);outer(entry);inner(entry);outer(exit);"}
  }
}
