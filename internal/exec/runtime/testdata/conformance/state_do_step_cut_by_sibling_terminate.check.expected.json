{
	"verdict": "divergent",
	"divergent": {
		"log": [
			"\"work(entry) l1(entry) l1(partI) r1(entry) \"",
			"\"work(entry) l1(entry) r1(entry) \"",
			"\"work(entry) l1(entry) r1(entry) l1(partI) \"",
			"\"work(entry) r1(entry) l1(entry) \"",
			"\"work(entry) r1(entry) l1(entry) l1(partI) \""
		]
	},
	"agreed": {}
}
