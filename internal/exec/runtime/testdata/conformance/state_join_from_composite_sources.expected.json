{
  "type": "state",
  "trace": true,
  "events": [{"signal": "Go"}],
  "outcomes": [
    {
      "finalState": "done",
      "outputs": {
        "log": {"type": "String", "value": "a1(exit) ia(exit) a b1(exit) b work(exit) sync"}
      }
    },
    {
      "finalState": "done",
      "outputs": {
        "log": {"type": "String", "value": "b1(exit) b a1(exit) ia(exit) a work(exit) sync"}
      }
    }
  ],
  "admissible": "Transitions into a join: each exits its source and runs its effect before the owner is left, in which order is open"
}
