{
  "type": "state",
  "schedule": "reverse",
  "trace": true,
  "events": [
    {"signal": "Up", "args": null},
    {"signal": "Finish", "args": null}
  ],
  "finalState": "done",
  "outputs": {
    "log": {"type": "String", "value": "outer(entry);inner(entry);a(entry);c(entry);b(entry);a(exit);c(exit);inner(exit);up(effect);b(exit);outer(exit);completion(effect);"}
  }
}
