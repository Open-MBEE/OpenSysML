{
	"type": "state",
	"trace": true,
	"events": [{"signal": "Abort"}],
	"terminated": true,
	"stateVisits": ["work", "inner"],
	"outputs": {
		"log": {"type": "String", "value": "work(entry);inner(entry);watch;inner(exit);abort(effect);"}
	}
}
