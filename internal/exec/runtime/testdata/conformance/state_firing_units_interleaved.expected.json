{
	"type": "state",
	"trace": true,
	"events": [
		{
			"signal": "Go"
		}
	],
	"outcomes": [
		{
			"finalState": "done",
			"outputs": {
				"log": {
					"type": "String",
					"value": "l1(exit) T1(effect) r1(exit) T2(effect) T3(effect) "
				}
			}
		},
		{
			"finalState": "done",
			"outputs": {
				"log": {
					"type": "String",
					"value": "l1(exit) r1(exit) T1(effect) T2(effect) T3(effect) "
				}
			}
		},
		{
			"finalState": "done",
			"outputs": {
				"log": {
					"type": "String",
					"value": "l1(exit) r1(exit) T2(effect) T1(effect) T3(effect) "
				}
			}
		},
		{
			"finalState": "done",
			"outputs": {
				"log": {
					"type": "String",
					"value": "r1(exit) T2(effect) l1(exit) T1(effect) T3(effect) "
				}
			}
		},
		{
			"finalState": "done",
			"outputs": {
				"log": {
					"type": "String",
					"value": "r1(exit) l1(exit) T1(effect) T2(effect) T3(effect) "
				}
			}
		},
		{
			"finalState": "done",
			"outputs": {
				"log": {
					"type": "String",
					"value": "r1(exit) l1(exit) T2(effect) T1(effect) T3(effect) "
				}
			}
		}
	],
	"admissible": "Transitions in sibling regions enabled by one event: each fires, in which order is open"
}
