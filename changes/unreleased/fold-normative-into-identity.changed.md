- **Normative library ids are minted in `identity` itself.** `internal/semantic/identity/normative` is folded into `internal/semantic/identity`: `normative.Language`, `KerML`, `SysML`, `ElementID`, `OwningMembershipID`, `EscapeName` and `NamespaceURL` are now `identity.Language`, `identity.KerML`, and so on. No behavior changes.
