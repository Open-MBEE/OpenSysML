- Documents gain an `Image` content block (`location` a path relative to the document's file or an http(s)/file URL, optional `caption` and `alt`), rendered as `![alt](location)` in Markdown, a `<figure class="sysml-image">` in HTML and the drawn image in PDF — a missing local file is a `missing-image` error naming the block.
- The SysML v1 migrator writes View Editor image paragraphs — comments stereotyped MagicDraw «AttachedFile», or carrying an `<img>` — as `DocumentQueries::Image` blocks, and copies the attached bytes beside the notation under `images/` when `-o` names a file (stdout or a Flexo target reports the requirement instead of dropping them); an attached file no archive entry holds is refused with the file named.
