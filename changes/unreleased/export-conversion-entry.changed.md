- **Conversions are driven from `internal/translate/convert`, not from the RDF mapping.** The format names, `Convert`, `ConvertTolerant`, `SysMLElement`, `Migrate`, `SysMLToRDF` and `SyntaxError` moved out of `internal/translate/export` into a conversion entry point that parses notation and runs the SysML v1 migration before handing the tree or graph to the mapping; `internal/translate/export` keeps `ToRDF` and `ToSysML` and no longer imports the migration. `sysml -convert`, `%save`, `%print` and the service's `Convert` behave as before, and a hygiene test keeps the mapping free of the migration.
